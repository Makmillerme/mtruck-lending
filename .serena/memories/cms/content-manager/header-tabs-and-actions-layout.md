Refined ContentManager layout per UX request. In components/admin/content-manager.tsx: (1) moved array action controls to the right side of array block header; top-level arrays now include Reset + Save next to Add. (2) Add action changed to compact square icon button with plus only (h-8 w-8). (3) active header element tabs now have strong active state classes (primary background/foreground + border). (4) in accordion rows, right-side action area is pushed fully to the right with ml-auto and contains chevron + delete controls; left side keeps clear title/subtitle content. (5) global reset/save row removed for array tabs and kept for non-array tabs.