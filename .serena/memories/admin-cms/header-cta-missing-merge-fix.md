Fixed missing Header CTA settings issue in components/admin/content-manager.tsx. Root cause: loaded DB section value replaced template fully, so missing keys (like header.cta) became undefined. Added recursive mergeWithTemplate(template, incoming) to preserve template defaults for absent keys while keeping existing DB values. Updated section load effect to merge found entries with section template for each locale.