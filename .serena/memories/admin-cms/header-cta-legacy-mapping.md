Addressed empty Header CTA fields due to legacy data shape. In components/admin/content-manager.tsx added normalizeSectionValue(section, value) compatibility mapper for header: maps legacy ctaText/ctaTargetId into modern cta.text/cta.targetId, and fills missing cta fields when cta object exists but is incomplete. Applied normalization before mergeWithTemplate in section loading effect.