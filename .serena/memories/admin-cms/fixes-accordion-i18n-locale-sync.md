Implemented user-requested fixes across admin CMS. In components/admin/content-manager.tsx: localized section tabs, group tabs, and technical field labels; localized shared technical field text, item text, empty list, and saving label. Reworked array accordion row so the trigger area is full-width and reliably expandable, with delete action pinned on the right in an absolute action zone. In components/admin/admin-dashboard.tsx: admin locale switcher now persists to localStorage key 'locale' and restores on load, matching site locale storage for cross-app sync. In components/admin/settings-manager.tsx: stabilized language switcher toggle UI to prevent button jumping by using fixed-width segmented controls with consistent dimensions.