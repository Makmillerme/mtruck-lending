Reworked components/landing/neon-avto-assemble.tsx to run animation directly in site (About section) without playground/fetch CORS dependence. Removed GSAP/fetch SVG pipeline. New client component uses canvas Sobel-based line particles from /images/neon_avto.png, smooth assemble/explode loop, convergence detection, then fades to final /images/neon_avto_assemble.svg with neon drop-shadow. Keeps automatic repeating cycle and fallback to final SVG if image decode fails. Lints clean.