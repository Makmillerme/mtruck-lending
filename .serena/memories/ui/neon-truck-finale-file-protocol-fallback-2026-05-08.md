Updated playground/neon-truck-finale/index.html to support file:// opening without crashing: loadAndPrepareSvg now tries fetch-inline SVG first, and on failure falls back to <img src='../../neon_avto.svg'> in overlay. Also expanded CSS selector from '#svg-overlay svg' to '#svg-overlay svg, #svg-overlay img' with object-fit contain and same glow animation. This avoids CORS fetch failure while preserving full inline-filter mode under http/https static server.