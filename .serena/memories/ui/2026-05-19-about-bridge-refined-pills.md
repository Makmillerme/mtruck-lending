## About bridge refined (integrated look)

Replaced timeline/checklist box (grid bg, vertical line, circles) with:
- Transparent bridge zone (no extra card border)
- Centered title with horizontal gradient rules
- 2x2 grid of `landing-pipeline-pill landing-pipeline-pill--compact` — same component family as Services + truck callouts
- `.landing-pipeline-pill--compact` allows wrap + smaller type

Files: `app/globals.css`, `components/landing/about.tsx`