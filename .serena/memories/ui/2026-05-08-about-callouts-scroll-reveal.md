About floating callouts: scroll-triggered reveal on the neon visual card. `about.tsx` uses `useRef` + `IntersectionObserver` (threshold 0.16, rootMargin bottom -10%) on the aspect-square container; after intersect + 340ms delay sets `calloutsInView`. Stagger via CSS var `--about-callout-stagger` (0/100/200ms). `AboutFloatingCallout` wraps chip in `.about-float-callout-inner` for float animation with `animation-play-state: paused` until `.about-float-callout--in-view`. `prefers-reduced-motion: reduce` shows callouts immediately (no observer delay path shows on mount). CSS updated in `globals.css`.