Created isolated demo at playground/neon-truck-finale/index.html plus README.md. Demo auto-loads ../../neon_avto.png, builds Sobel-based capped particles, supports explode/assemble controls, detects convergence, then crossfades to inline-loaded ../../neon_avto.svg with neonGlowFilter and pulse glow. Important runtime note: must be served via static server; file:// can block SVG fetch due to CORS/security. No changes in src/ app/ or package.json.